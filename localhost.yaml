---
- hosts: 127.0.0.1
  connection: local
  vars:
    author: freezippo
  tasks:
  - name: Create user
    action: user name=freezippo comment="Freezippo" shell=/bin/bash createhome=yes home=/home/freezippo

